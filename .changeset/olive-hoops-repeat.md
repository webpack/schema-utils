---
"schema-utils": patch
---

Updated `ajv` to `8.20.0`, `ajv-formats` to `3.0.1` and `@types/json-schema` to `7.0.15`. `ajv-formats` 3 only moves `ajv` to a peer dependency, the formats themselves are unchanged.
